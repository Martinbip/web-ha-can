{
  "name": "web-ha-can",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/regression.test.js tests/admin-ui-config.test.js tests/admin-app.test.js tests/admin-dashboard.test.js tests/news-editor.test.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "admin:install": "npm install --prefix admin",
    "admin:dev": "npm run dev --prefix admin",
    "admin:build": "npm run build --prefix admin"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "dotenv": "^17.4.2"
  }
}
